package com.taller.motostock.core.common.mvi

/**
 * Interface base para el estado de la UI.
 */
interface UiState

/**
 * Interface base para las acciones del usuario (Intents).
 */
interface UiIntent

/**
 * Interface base para efectos secundarios de un solo disparo (navegación, toasts, etc.).
 */
interface UiEffect
